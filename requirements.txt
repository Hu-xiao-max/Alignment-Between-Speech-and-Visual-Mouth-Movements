# LipNet Training Requirements
# installation: pip install -r requirements.txt

#core dependencies
tensorflow>=2.10.0
numpy>=1.21.0
opencv-python>=4.5.0
matplotlib>=3.5.0
scikit-learn>=1.0.0
tqdm>=4.64.0

# optional: lip detection (if dlib is not installed, simple region cropping will be used)
# dlib needs cmake and build environment, may need to install separately:
# pip install dlib --no-cache-dir --force-reinstall --no-build-isolation

# optional: for better evaluation metrics
# editdistance>=0.6.0
